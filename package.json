{
  "name": "@signalk/sailsconfiguration",
  "version": "1.2.0",
  "description": "Signal K plugin for setting the vessel's sail inventory and configuration",
  "main": "index.js",
  "scripts": {
    "build": "tsc validator.ts && js-yaml openapi.yaml > openapi.json",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "signalk-node-server-plugin",
    "signalk-category-utility",
    "signalk-webapp"
  ],
  "author": "teppo.kurki@iki.fi",
  "license": "ISC",
  "devDependencies": {
    "js-yaml": "^4.1.0"
  },
  "dependencies": {
    "fluentvalidation-ts": "^5.0.0"
  }
}
